import 'package:flutter/material.dart';

class Spenden extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
        'Unterstütze uns, Simon!',
        style: TextStyle(fontSize: 20, fontFamily: 'Abadi'),
      ),
    );
  }
}
